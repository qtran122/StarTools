"""Common math functions that are generally useful for all math programs.
Currently, this module simply serves as an example for how to import common 
libraries and has no true useful utility yet.
"""

def add_one(num1):
    """A simple function for incrementing. Used to show successful importing"""
    print ("-- math_utils.py : add_one() called")
    return num1 + 1
